agent: contract-reviewer
change-id: hold-history-rq-async
timestamp: 2026-06-13
status: complete

artifacts:
  - { type: contracts-reviewed, pointer: "contracts/api/api-contract.md (1.17.0→1.18.0)" }
  - { type: contracts-reviewed, pointer: "contracts/api/api-inventory.md (1.2.0→1.2.1)" }
  - { type: contracts-reviewed, pointer: "contracts/env/env-contract.md (1.0.9→1.0.10)" }
  - { type: contracts-reviewed, pointer: "contracts/ci/ci-gate-contract.md (1.3.21→1.3.22)" }
  - { type: contracts-reviewed, pointer: "contracts/data/data-shape-contract.md (no update — async result envelope identical to sync)" }
  - { type: breaking-changes, pointer: "none — all changes additive" }
  - { type: consumers-impacted, pointer: "frontend/src/hold-history/ (internal only)" }

notes: |
  data-shape-contract.md: no update required. Worker wraps execute_primary_query();
  existing §1.3, §1.4 cover async envelope; hold_dataset spool already documented.
  Parity test (AC-3) is the tripwire if this assumption reverses.
  Flag-default risk noted: HOLD_ASYNC_ENABLED=true means all 90-day+ queries go async
  on first deploy if worker is not started first — is_async_available() fallback handles this.
