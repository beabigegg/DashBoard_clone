agent: qa-reviewer
change-id: unified-query-core-infra
decision: blocked
summary: >-
  Tier-1 QA gate review. AC-1..AC-8 functionally verified against the 3 new core
  modules; 62 targeted tests pass on independent reproduction; test-evidence.yml
  is complete (collect/targeted/changed-area/contract/full all passed, no
  waivers, generated by cdd-kit test run). BLOCKED on two required-gate failures:
  (1) cdd-kit gate tier-floor violation — concurrency-critical surface
  (writer_lock, ThreadPoolExecutor, Oracle session pool) requires tier 0 but
  classification declares tier 1 with no tier-floor-override; (2) ruff lint
  F401 unused import `dataclasses.field` in query_cost_policy.py (new file,
  introduced by this change).
artifacts:
  - { type: gate-results, pointer: "targeted-pytest: pass (62), test-evidence: pass, contract-validate: not-run-standalone, cdd-kit-gate: FAIL (tier floor), lint(ruff): FAIL (F401)" }
  - { type: ci-run-url, pointer: "n/a (local-only)" }
  - { type: evidence-quality, pointer: "log" }
  - { type: decision, pointer: "blocked" }
  - { type: failure-routing, pointer: "tier misclassification -> change-classifier + spec-architect; lint F401 -> backend-engineer; sample-json drift -> test-strategist (note only)" }
ac-findings:
  - { ac: AC-1, status: pass, note: "ChunkStrategy enum has TIME/ID_LIST/ROW_COUNT/SINGLE; run() invokes pre_query->fan_out(build_chunk_sql+chunk_to_duckdb)->post_aggregate with progress brackets 5/15/90/100." }
  - { ac: AC-2, status: pass, note: "requires_cross_chunk_reduction=False -> _fan_out_append; =True -> _fan_out_reduction + _writer_lock into single job-temp duckdb. Append path's parquet write delegated to subclass override (no caller yet; acceptable for base)." }
  - { ac: AC-3, status: pass, note: "_pool class attr None at import; _init_pool deferred oracledb import; min=2/max=15; finally: conn.close(); yields pa.RecordBatch." }
  - { ac: AC-4, status: pass, note: "classify_query_cost L0 spool->SYNC, L1 always-async, L2 date_span>=threshold, L3 row_count_fn>=200k, short-circuit order correct; row_count_fn only called past L2." }
  - { ac: AC-5, status: pass, note: "_make_job_duckdb_path = {DUCKDB_JOB_DIR}/{namespace}/{job_id}.duckdb; _cleanup in run() finally on success and error." }
  - { ac: AC-6, status: pass, note: "DUCKDB_JOB_DIR in env-contract.md (schema 1.0.14), .env.example.template, env.schema.json; default tmp/duckdb_jobs == {QUERY_SPOOL_DIR}/../duckdb_jobs; contract test passes." }
  - { ac: AC-7, status: partial, note: "No pandas import (docstring mentions only); no callers of 3 modules in src/. BUT lint(ruff) fails F401 in query_cost_policy.py. Also 171 tests/contract/samples/*.json show runtime-volatile drift (timestamps/memory/live error snapshots) outside declared 3+4+5 file scope — not produced by this change's code, no behavior change; routed as note." }
  - { ac: AC-8, status: pass, note: "_strip_char applied per column in chunk_iter(); DATE midnight regex documented; data-shape contract section 'Oracle -> RecordBatch -> DuckDB/parquet Streaming Boundary' added (data 1.19.0) with 9 invariants." }
blocking-issues:
  - { id: B1, type: misclassification, gate: cdd-kit-gate, detail: "tier floor violation: concurrency surface requires tier 0; tier 1 declared; no tier-floor-override in tasks.yml frontmatter.", route: "change-classifier + spec-architect", fix: "re-classify to tier 0 OR add tier-floor-override (>=20 chars) to tasks.yml frontmatter with audit reason." }
  - { id: B2, type: test-gap, gate: lint, detail: "ruff F401 unused import dataclasses.field at query_cost_policy.py:23 (new file).", route: "backend-engineer", fix: "remove unused 'field' import; rerun ruff check ." }
non-blocking-notes:
  - { id: N1, detail: "171 tests/contract/samples/*.json modified (timestamps, memory readings, live Oracle error snapshots). Volatile harness regeneration drift, not from the 3 core modules. Recommend reverting/excluding before commit to keep diff confined per AC-7.", route: "test-strategist" }
  - { id: N2, detail: ".cdd/code-map.yml modified by index refresh during review; expected, not a finding." }
  - { id: N3, detail: "Sign-off rule: high/critical risk needs qa-reviewer + spec-architect. This is high risk -> a second reviewer (spec-architect) is required before any eventual approval; moot while blocked." }
required-phases-verified: [collect, targeted, changed-area, contract, full]
final-status-in-evidence: passed
