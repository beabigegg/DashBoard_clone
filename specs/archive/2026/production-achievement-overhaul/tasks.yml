change-id: production-achievement-overhaul
status: done
tier: 0
context-governance: v1
archive-tasks:
  - "7.1"
  - "7.2"
depends-on: []

tasks:
  # status: pending | done | skipped   (optional: note: "reason or context")
  - { id: "1.1", section: Preparation, title: "Confirm classification and required artifacts", status: done }
  - { id: "1.2", section: Preparation, title: "Confirm contracts to update", status: done }
  - { id: "1.3", section: Preparation, title: "Confirm design decisions if required", status: done }
  - { id: "1.4", section: Preparation, title: "Confirm CI/CD gate plan", status: done }
  - { id: "1.5", section: Preparation, title: "Confirm implementation plan", status: done }
  - { id: "2.1", section: "Contract Updates", title: "API contract", status: done }
  - { id: "2.2", section: "Contract Updates", title: "CSS/UI contract", status: done, note: "frontend-engineer added .theme-production-achievement-settings scope + chart-replacement rules; css-contract.md 1.16.0 / css-inventory.md 1.2.12. npm run css:check: 0 errors." }
  - { id: "2.3", section: "Contract Updates", title: "Env contract", status: skipped, note: "No new env vars — reuses existing WARMUP_INTERVAL_SECONDS/WARMUP_SCHEDULER_ENABLED/QUERY_SPOOL_TTL_SECONDS/PRODUCTION_ACHIEVEMENT_USE_UNIFIED_JOB" }
  - { id: "2.4", section: "Contract Updates", title: "Data shape contract", status: done }
  - { id: "2.5", section: "Contract Updates", title: "Business logic contract", status: done }
  - { id: "2.6", section: "Contract Updates", title: "CI/CD contract", status: skipped, note: "No contracts/ci/ci-gate-contract.md change — no new RQ worker service, New RQ Worker Deploy Checklist gate not triggered" }
  - { id: "3.1", section: "Tests First", title: "Unit/contract tests", status: done, note: "Backend: 258 passed (routes/services/worker/contract). Frontend TDD in progress." }
  - { id: "3.2", section: "Tests First", title: "Integration tests", status: done, note: "Backend: MySQL roundtrip (package_lf/workcenter_merge/daily_plan tables), filter_cache reuse, resilience -- all passing." }
  - { id: "3.3", section: "Tests First", title: "E2E/resilience tests", status: done, note: "e2e-resilience-engineer: fixed a real selector bug (data-testid mismatch, 4 spec files) plus 10 new tests (CumulativeView/D3, D6 backstop, OD-4 real ignore-mid-poll, D5 shared machinery, D1 vs D2 contrast, 401 mid-poll, 300-row payload, wrong-type inline maps, OD-7 persistence). Found a real build-breaking bug (4 missing tailwind.config.js colors.token.h* entries used by production-achievement-settings/style.css and production-achievement/style.css) -- fixed directly by main Claude; npm run build/css:check both clean now. 44 tests parse cleanly across 6 spec files; cannot execute in this sandbox (Playwright chromium cache is v1228, repo pins v1208 -- installing a new version here is against ci-workflow.md's CI-runner-only policy; CI will run them for real)." }
  - { id: "3.4", section: "Tests First", title: "Data-boundary/monkey tests", status: done, note: "monkey-test-engineer: diffed against the pre-overhaul monkey spec and reinstated 3 genuinely-dropped scenarios (TargetEditPanel rapid-double-click, adversarial range-date input, 6/8 target_qty adversarial classes) plus strengthened the mode-mashing test to a real multi-tick 202-poll race. 2 real low-severity findings documented via passing hardening tests (DailyPlanPanel stale-dropdown-value race; useProductionAchievementSettings.ts shared editSaving flag has no reentrancy guard across panels) -- both idempotent/no-FK/no-corruption, consistent with this codebase's existing TargetEditPanel precedent for similar-severity issues; not blocking. 932 vitest passed repo-wide, 49 Playwright tests parse clean across 6 files." }
  - { id: "3.5", section: "Tests First", title: "Stress/soak tests if required", status: done, note: "stress-soak-engineer: extended test_production_achievement_stress.py (8-warmup-job no-monopolization/fault-isolation, 4 new tests) and test_chunk_boundary.py (D6 closing-chunk correctness at 30/90/180-day scale, zero leakage/duplication across every seam). Found+fixed a pre-existing (not this session's) schema-staleness bug in the stress file's own parquet-writer helper. Soak correctly skipped per test-plan.md classification; stress-soak-report.md correctly not created (classification says no, results not concerning)." }
  - { id: "4.1", section: Implementation, title: "Backend", status: done, note: "backend-engineer complete: Phases 1-5 (SQL/D6 fix/schema-v2/3 MySQL tables/services/10 routes/warm-cache). 3 deliberate deviations documented in agent-log (warmup-job naming to avoid regression-test collision; bare Name[] schema notation + widened validator, not a contract edit; asset-readiness env-flag collision traced to concurrent frontend agent, confirmed zero backend regression)." }
  - { id: "4.2", section: Implementation, title: "Frontend", status: done, note: "frontend-engineer complete: Phases 6-9 (2-stage DuckDB rollup, 4-mode composable, App.vue+chart rewrite, settings mini-app, 7-location route registration). Independently found and fixed the same api-contract.md envelope-schema gap backend-engineer had flagged as a blocking failure -- reconciled, both now consistent. Also introduced (fixed by main Claude) a build-breaking tailwind.config.js token gap -- see 3.3 note." }
  - { id: "4.3", section: Implementation, title: "Env/deploy", status: skipped, note: "No new env vars, no new deploy/*.service file — existing warmup RQ worker already runs generically against the warmup queue" }
  - { id: "4.4", section: Implementation, title: "CI/CD workflows", status: done, note: "No new workflow FILE, but ci-cd-gatekeeper wired 5 new steps into the existing .github/workflows/frontend-tests.yml (closing a pre-existing gate-name-declared-but-never-wired gap for playwright-critical-journeys/resilience/data-boundary, plus 2 brand-new gates: production-achievement-monkey, production-achievement-settings-e2e). See ci-gates.md Workflow section." }
  - { id: "5.1", section: Review, title: "UI/UX review", status: done, note: "ui-ux-reviewer: approved, no blocking findings. All 12 confirmed OD's verified against actual code. 6 advisory findings; fixed 1 directly (internal change-id slug leaking into PermissionsTab.vue admin copy, verified no test asserted the string, re-ran 8/8 passing). See agent-log for the rest (stale-results-undimmed, no disabled-state cue, 3 trivial cosmetic nits) -- all design-silent, none blocking." }
  - { id: "5.2", section: Review, title: "Visual review", status: done, note: "visual-reviewer: approved, no blocking findings. Independently re-verified main Claude's tailwind.config.js token fix 3 ways (static, fresh build, compiled-CSS output) -- all sound. css-contract.md Rule 4.2/4.3/2.4 compliant, css:check 0 errors. 2 advisory non-blocking nits. See visual-review-report.md." }
  - { id: "5.3", section: Review, title: "Contract review", status: done, note: "Post-implementation reconciliation: fixed css-contract.md/css-inventory.md missing CHANGELOG.md entries (1.16.0/1.2.12), corrected the api 1.41.0 changelog entry to reflect frontend-engineer's envelope-wrapper schema fix, reverted ~175 unrelated tests/contract/samples/*.json files accidentally regenerated with live-env noise (kept only this change's 10 new samples), and rewrote interaction-design.md's Presented Information/States citations to the strict ADR 0012 §2 5-form grammar (was free prose) plus checked all 12 Open Decision boxes. cdd-kit validate --contracts --versions and cdd-kit gate both clean except the pre-existing human-only acceptance.yml placeholder gap." }
  - { id: "5.4", section: Review, title: "QA review", status: done, note: "qa-reviewer: decision=blocked. Re-ran every gate independently (all green except gate --strict, which fails on pending tasks + the human-only acceptance.yml oracle). Found 3 new gaps prior agents missed: (1) planned D3 property test (tests/property/test_production_achievement_aggregate_invariant.py) never actually created despite test-strategist's log claiming it; (2) tests/test_frontend_production_achievement_parity.py is a stale holdover from the PRIOR change, 0 PACKAGE_LF refs, tests deleted SQL; (3) regression-report.md required but missing. main Claude wrote regression-report.md directly (re-verified all 4 required regression areas live); dispatched backend-engineer fix-back for (1)/(2)." }
  - { id: "6.1", section: Verification, title: "Local gates", status: done, note: "cdd-kit validate --contracts --versions: pass. cdd-kit gate production-achievement-overhaul: fails only on the human-only acceptance.yml oracle (ADR 0010 -- cannot be closed by any agent). Full unscoped pytest (5507 tests, no -k filter): 5506 passed, 1 pre-existing unrelated failure (test_runtime_hardening.py, confirmed zero connection to this change), 734 skipped, 1 xfailed. npm run build/css:check/vitest all clean." }
  - { id: "6.2", section: Verification, title: "PR required gates", status: done, note: "contract validate, openapi sync (both contracts/openapi.json + contracts/api/openapi.json re-verified in sync), full backend pytest, frontend vitest, css:check all directly re-run and clean. The 5 Playwright gates (critical-journeys/async/resilience/data-boundary/monkey/settings-e2e) parse-clean (49 tests, 6 files, --list) but cannot execute locally (Chromium v1228 cached vs v1208 pinned in this sandbox; installing a new version is CI-runner-only per ci-workflow.md) -- true PR-gate execution happens in CI, same ceiling every test agent hit this session." }
  - { id: "6.3", section: Verification, title: "Informational gates", status: done, note: "Property gate (pytest -m property, HYPOTHESIS_PROFILE=ci): 47 passed incl. the new D3 aggregate-invariant test. type-check informational per repo-wide policy (continue-on-error), unaffected by this change. visual-regression gate not implemented repo-wide; visual-review-report.md serves as this change's interim visual evidence per change-classification.md." }
  - { id: "6.4", section: Verification, title: "Nightly/weekly/manual gates if required", status: done, note: "stress (manual dispatch, Tier 5): exercised directly by stress-soak-engineer, all passing. soak (weekly cron): explicitly not required for this change per test-plan.md classification, nothing to wire. nightly-integration-real: not applicable, all production_achievement integration tests are mock-bounded (no integration_real marker)." }
  - { id: "5.5", section: Review, title: "QA fix-back cycle 1", status: done, note: "backend-engineer: created tests/property/test_production_achievement_aggregate_invariant.py (exact closed-form identity, not probabilistic -- verified via Fraction arithmetic) and rewrote tests/test_frontend_production_achievement_parity.py in place for the widened PACKAGE_LF grain (2 classes: Stage1RollupParity + full-grain RollupParity incl. D1+D2 in one shared fixture, test_multiple_package_lf_per_specname_day_parity per test-plan.md). Deliberately dropped the old target-based TestProductionAchievementRateParity class since that join no longer exists in the current frontend (verified: TargetEditPanel.vue has zero achievement_rate references; pa_targets_map is registered in DuckDB but never SELECTed/JOINed anywhere). 18/18 new/rewritten tests passing, zero regressions in the changed-area sweep. Flagged (deferred, out of scope): business-rules.md PA-07's Test Reference/Implementation-locus text describing a browser dual-tier parity test against build_achievement_rows() is itself stale -- pre-dates this change (from the already-archived production-achievement-async-spool change), and PA-11 explicitly states PA-07 is unaffected by this change's scope. Independently confirmed via direct grep (TARGETS_TABLE/pa_targets_map registered but never queried). Left for a future, separately-scoped contract correction -- not blocking, not introduced by this change.\n\nMain Claude then ran the FULL unscoped pytest suite (no -k filter, 5507 tests) per CLAUDE.md's own \"full pytest locally before pushing a removal/shape change\" discipline -- something no prior agent had done (all prior runs were scoped to -k production_achievement or similar). Found a SECOND, previously-unknown hardcoded warmup-job-count test outside this change's own test files: tests/test_unified_spool_integration.py::TestProductionHistoryWarmupExclusion::test_warmup_job_count_is_exactly_six asserted len(_WARMUP_JOBS)==6, never updated for this change's 6->8 job count (distinct from the sibling test in test_spool_warmup_scheduler.py, which WAS updated). Fixed directly (renamed to test_warmup_job_count_is_exactly_eight, assertion updated to 8, docstring lists both new jobs) -- now 3/3 passing in that test class. The suite's only OTHER failure, tests/test_runtime_hardening.py::test_health_reports_pool_saturation_degraded_reason, is confirmed pre-existing and unrelated: zero production_achievement references, neither that file nor mysql_client.py has any modification this session (git status clean), and it depends on already-committed MySQL-pool-sizing code from before this change started -- not fixed, not this change's responsibility, flagged to the user as a pre-existing repo issue. Final full-suite result: 5506 passed (up from 5505), 1 pre-existing unrelated failure, 734 skipped, 1 xfailed." }
  - { id: "7.1", section: Archive, title: "Archive change", status: done }
  - { id: "7.2", section: Archive, title: "Promote durable learnings to contracts or CLAUDE.md", status: done, note: "3 lessons promoted via contract-reviewer: (1) Boundary Guard shadow_mode gap -- CLAUDE.md bullet extended + docs/cdd-kit-patterns.md updated; (2) Playwright waitForFunction must be gated not timeout-bounded -- CLAUDE.md bullet tightened + docs/architecture/ci-workflow.md evidence appended; (3) Gate Inventory presence != proof of wiring -- new contracts/ci/ci-gate-contract.md section, schema-version 1.3.39->1.3.40, contracts/CHANGELOG.md [ci 1.3.40] entry. See archive.md ## Lessons Promoted to Standards for full detail. cdd-kit validate --contracts --versions passed after all edits." }
