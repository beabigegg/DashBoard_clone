agent: contract-reviewer
change-id: wip-rq-worker-chunks-cleanup
timestamp: 2026-06-20
status: changes-required

summary: >
  API and env contracts both require updates. No breaking changes.
  All 10 blocking items assigned to backend-engineer work packet.

artifacts:
  - { type: contracts-reviewed, pointer: "contracts/api/api-contract.md" }
  - { type: contracts-reviewed, pointer: "contracts/env/env-contract.md" }
  - { type: contracts-reviewed, pointer: "contracts/env/env.schema.json" }
  - { type: version-bumps, pointer: "api-contract 1.25.1→1.26.0 (minor, additive); env-contract 1.0.21→1.0.22 (patch, additive)" }
  - { type: breaking-changes, pointer: "none" }

notes: |
  API blocking items (backend-engineer):
    1. Add wip_dataset to /api/spool/{namespace}/{query_id}.parquet namespace allowlist (HIGH — HTTP 400 regression)
    2. Add 202 to WIP detail endpoint rows error column (HIGH)
    3. Add WipDetailJobAccepted Tier-B schema to ## Schemas (MEDIUM — ADR 0007)
    4. Add wip_routes.py to Section 7 Type B 適用 list (MEDIUM)
    5. Add Section 10 Compatibility Note (LOW)
    6. Regen contracts/api/openapi.json via cdd-kit openapi export (HIGH — CI gate)
  Env blocking items (backend-engineer):
    7. Add ## Async Worker — WIP Detail Query section to env-contract.md with WIP_WORKER_QUEUE/WIP_JOB_TIMEOUT_SECONDS/WIP_SPOOL_TTL (HIGH)
    8. Add three WIP vars to env.schema.json (HIGH — CLAUDE.md rule)
    9. Add version bumps to contracts/CHANGELOG.md (MEDIUM)
  Test item:
    10. Parametrize tests/test_spool_routes.py with wip_dataset in same PR (HIGH — CLAUDE.md rule)
  Default values (inferred from sibling patterns, backend-engineer to confirm):
    WIP_WORKER_QUEUE: "wip-detail-query"
    WIP_JOB_TIMEOUT_SECONDS: 1800
    WIP_SPOOL_TTL: 72000
  AC-7 spool schema parity: flag-only (no data-shape contract change required;
    backend-engineer must confirm async assembled response is byte-equivalent to sync response shape)
