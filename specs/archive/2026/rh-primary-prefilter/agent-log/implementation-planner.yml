change-id: rh-primary-prefilter
agent: implementation-planner
timestamp: "2026-06-25T00:00:00Z"
status: blocked
artifacts:
  - { type: plan-written, pointer: "specs/changes/rh-primary-prefilter/implementation-plan.md" }
  - { type: owner-map, pointer: "backend-engineer, frontend-engineer" }
  - { type: scope-summary, pointer: "14 IPs; 5 CER-gated; PJ_BOP out of scope" }
  - { type: blocked-reason, pointer: "CER-1..CER-5 pending: BASE_WHERE injection + spool/cache key + both async job paths outside Allowed Paths" }
next-action: "User runs cdd-kit context approve rh-primary-prefilter CER-1 (and CER-2..CER-5), then backend-engineer begins IP-2/IP-3/IP-9/IP-10"
notes: >
  Plan is complete and self-contained but execution of the parity-critical IPs
  (RHPF-05) requires reject_dataset_cache.py, reject_query_job_service.py,
  workers/reject_history_worker.py, and their two test files — none in the current
  context-manifest Allowed Paths. Five Context Expansion Requests filed.
  IP-1, IP-4..IP-8, IP-11..IP-14 are unblocked within current allowed paths.
indexes-used:
  - .cdd/code-map.yml
