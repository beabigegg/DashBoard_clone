change-id: remove-unused-pages
agent: frontend-engineer
date: 2026-05-18

artifacts:
  - type: files-changed
    pointer: "contracts/css/css-inventory.md (schema-version 1.2.1, deleted 3 rows)"
  - type: files-changed
    pointer: "contracts/CHANGELOG.md (css-inventory 1.2.1 entry prepended)"
  - type: files-changed
    pointer: "frontend/vite.config.ts:31 (removed tables entry; added production-history)"
  - type: files-changed
    pointer: "frontend/src/portal-shell/routeContracts.js (removed /tables from IN_SCOPE_REPORT_ROUTES and ROUTE_CONTRACTS)"
  - type: files-changed
    pointer: "frontend/src/portal-shell/nativeModuleRegistry.js (removed /tables loader block)"
  - type: files-changed
    pointer: "frontend/src/portal-shell/style.css (removed .tables-page rules from multi-selector and standalone block)"
  - type: files-changed
    pointer: "frontend/tests/legacy/loading-standardization.test.js (deleted 2 DataViewer test blocks referencing tables/)"
  - type: files-changed
    pointer: "frontend/src/admin-user-usage-kpi/components/ (restored; needed by admin-dashboard/tabs/UsageTab.vue)"
  - type: components-affected
    pointer: "portal-shell routeContracts, nativeModuleRegistry, style.css"
  - type: screenshot-paths
    pointer: "n/a (no visual surface changes)"
  - type: accessibility-audit
    pointer: "n/a (no interactive element changes)"

notes:
  - "DISCOVERY: frontend/src/admin-dashboard/tabs/UsageTab.vue imports 7 components from admin-user-usage-kpi/components/. The implementation plan did not account for this dependency. Restored admin-user-usage-kpi/components/ from HEAD to fix the build. SPA entry files (App.vue, main.js, index.html, style.css) remain deleted; only the components/ subdirectory is kept."
  - "css:check exits non-zero due to 6 pre-existing HEX violations in query-tool/components/EquipmentRejectsTable.vue (from equipment-rejects-by-lots change). Not introduced by this change."
  - "BUILD EVIDENCE: vite build exits 0; production-history.js produced at May 18 17:21; no new tables/admin-performance/admin-user-usage-kpi entry chunks produced."
  - "sidebarState.js, navigationState.js, router.js, routeQuery.js: grepped clean — zero residual references."
  - "package.json build script still has stale cp for tables.html — the stale dist/src/tables/index.html exists from previous build so cp succeeds. This is a separate pre-existing debt item outside Allowed Paths."

build-verification:
  - "npm run build: exit 0"
  - "npm run test: 33 files passed, 346 tests passed, 1 skipped"
  - "npm run type-check: exit 0 (no errors)"
  - "npm run css:check: exit 1 — 6 pre-existing HEX errors in query-tool (not introduced here)"
  - "cdd-kit validate: All validations passed"
