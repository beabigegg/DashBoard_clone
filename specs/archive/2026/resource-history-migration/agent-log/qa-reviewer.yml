agent: qa-reviewer
session: 2026-06-19
status: changes-required-then-resolved

findings:
  - id: QA-1
    severity: blocking
    resolved: true
    location: "tests/test_resource_history_service.py line 505"
    issue: "Duplicate class TestExportCsv shadows original at line 401; test_successful_export (AC-1 regression guard) not collected"
    fix: "Renamed duplicate class to TestFlagOffRegression"
  - id: QA-2
    severity: blocking
    resolved: true
    location: "tests/test_resource_history_service.py TestDegradedPath"
    issue: "test_flag_on_no_worker_returns_503 had no assertion (just read the flag value)"
    fix: "Converted to pytest class (no unittest.TestCase); uses client + monkeypatch fixtures; calls GET /api/resource/history/export with valid dates; asserts resp.status_code == 503"
  - id: QA-3
    severity: blocking
    resolved: true
    location: "tests/test_resource_history_job_service.py test_flag_on_enqueues_oee_job/both"
    issue: "Tests called _mock_enqueue() directly (tautological); never invoked real route"
    fix: "Both tests now use Flask test client + monkeypatch (RESOURCE_HISTORY_USE_UNIFIED_JOB=True) + patch aqs.enqueue_query_job; assert called_types contains both job strings after real GET request"
  - id: QA-R1
    severity: risk-note
    resolved: false
    location: "tests/integration/test_resource_history_rq_async.py"
    issue: "±30d OEE seam cross-chunk correctness only in integration_real stub (skipped). Must gate flag-on promotion (nightly Tier 3 per ci-gates Promotion Policy)"

artifacts:
  - { type: test-output, pointer: "142 targeted tests pass (up from 140; 2 tautological replaced with real route tests)" }
  - { type: test-output, pointer: "pytest tests/test_resource_history_service.py tests/test_resource_history_job_service.py: 43 passed" }
  - { type: ac-coverage, pointer: "AC-1: test_successful_export now collected (class renamed); AC-5: real route assertions; AC-7: 503 status code asserted; AC-3 seam: deferred to integration_real (flag-on gate)" }

final-verdict: approved
