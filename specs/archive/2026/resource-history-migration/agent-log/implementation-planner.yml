agent: implementation-planner
session: 2026-06-19
status: complete

artifacts:
  - { type: plan-written, pointer: specs/changes/resource-history-migration/implementation-plan.md }
  - { type: owner-map, pointer: "backend-engineer (all IP-1..IP-7)" }
  - { type: scope-summary, pointer: "2 new worker files, 3 service/route edits, 4 contract edits, test files per test-plan; no new systemd unit; no new CI workflows" }

key-findings:
  - "Real job service file is resource_query_job_service.py — NOT resource_history_job_service.py (manifest/tasks had wrong name; context-manifest corrected)"
  - "SQL files base_facts.sql + oee_facts.sql already date-parameterized; oee_facts.sql already has :reject_start/:reject_end binds"
  - "iterrows loop: OEE groupby l.521-531 + per-row availability/yield l.578-617; DuckDB SQL replacement must match formulas exactly (AC-4)"
  - "resource_oee namespace: NOT in spool_routes._ALLOWED_NAMESPACES and must stay absent (internal read only, never HTTP)"
  - "No new systemd unit: reuse resource-history-query queue; deploy checklist note for flag parity"
  - "always_async=True for both jobs (like EapAlarm); two enqueues per export call (AC-5)"
  - "OEE ±30d reject window is highest risk: per-chunk in build_chunk_sql"

next-action: backend-engineer implements IP-1..IP-7 in order
