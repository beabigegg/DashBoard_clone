agent: frontend-engineer
change-id: msd-forward-cause-effect
run-date: 2026-06-30
status: done

summary: |
  Replaced the MSD forward analysis view: removed Sankey/Heatmap hero toggle +
  3 downstream ParetoCharts + cross-filter banner; added ForwardReasonMatrix
  heat-table component; removed all dead forward-selection state and imports;
  updated Playwright specs and added vitest unit tests (16 pass).

artifacts:
  - type: files-changed
    pointer: "frontend/src/mid-section-defect/components/ForwardReasonMatrix.vue:1-345 (created)"
  - type: files-changed
    pointer: "frontend/src/mid-section-defect/App.vue (edited — imports, types, template, removed forward cross-filter machinery)"
  - type: files-changed
    pointer: "frontend/src/mid-section-defect/style.css (edited — removed forward-hero-section/card/toggle, forward-selection-banner, clear-selection rules)"
  - type: files-changed
    pointer: "frontend/src/mid-section-defect/components/ForwardFlowChart.vue (deleted)"
  - type: files-changed
    pointer: "frontend/src/mid-section-defect/components/ForwardHeatmap.vue (deleted)"
  - type: files-changed
    pointer: "frontend/tests/playwright/mid-section-defect.spec.ts (edited — replaced Sankey/heatmap tests with matrix tests, updated MOCK_FORWARD_ANALYSIS_DATA)"
  - type: files-changed
    pointer: "frontend/tests/legacy/mid-section-defect-composables.test.js (edited — replaced removed handleSankeyNodeClick test)"
  - type: files-changed
    pointer: "frontend/tests/components/ForwardReasonMatrix.test.js (created — 16 vitest tests)"
  - type: components-affected
    pointer: "ForwardReasonMatrix (new), App.vue mid-section-defect (template/script), ForwardFlowChart (deleted), ForwardHeatmap (deleted)"
  - type: screenshot-paths
    pointer: "n/a (no screenshots taken — UI is a table component, tests cover render)"
  - type: accessibility-audit
    pointer: "table uses th scope=col/row, aria-label on table element, toggle buttons have aria-pressed; no axe-core run (no browser available)"

verification:
  type-check: "pass (vue-tsc --noEmit, 0 errors)"
  build: "pass (vite build, 0 errors, 12.5s)"
  css-check: "pass (0 errors, 307 pre-existing warnings — none from new code)"
  test-vitest: "ForwardReasonMatrix.test.js: 16/16 pass; full suite: 649 pass / 1 pre-existing fail in yield-alert (unrelated)"
  test-legacy: "270/270 pass (node:test)"

python-e2e-files-with-removed-testids: |
  NONE — grep of tests/**/*.py found no references to toggle-sankey, toggle-heatmap,
  forward-hero, forward-selection-banner, forward-clear-selection, forward-flow-chart,
  forward-heatmap, ForwardFlowChart, ForwardHeatmap.

cross-filter-machinery-kept-inert: |
  None kept inert. All forward cross-filter state (forwardSelection, forwardHeroView,
  sankeyToggleBtnRef, ForwardSelection type, handleSankeyNodeClick, clearForwardSelection,
  filteredByDetectionLossReason, handleForwardLossReasonBarClick) was fully removed.
  byDetectionLossReasonChartData was simplified (cross-filter logic removed, now reads
  directly from by_detection_loss_reason). nextTick import removed as no longer used.

notes: |
  - hero-toggle-btn CSS class kept in style.css (reused by ForwardReasonMatrix toggle buttons).
  - FrontDownstreamReasonMatrix interface added to App.vue and mirrored in ForwardReasonMatrix.vue.
  - by_front_downstream_reason_matrix is optional (handles undefined/null with empty-state).
  - The pre-existing yield-alert test failure (test_process_type_selector_propagates_to_all_views)
    is unrelated to this change (fails on postCallCount assertion in a different app).
