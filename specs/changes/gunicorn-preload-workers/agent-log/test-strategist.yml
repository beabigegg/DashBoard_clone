agent: test-strategist
date: 2026-06-05
change-id: gunicorn-preload-workers

artifacts:
  - { type: test-plan-path, pointer: "specs/changes/gunicorn-preload-workers/test-plan.md" }
  - { type: tdd-pairs, pointer: "tests/test_post_fork_reinit.py ↔ src/mes_dashboard/app.py (post_fork hook) + src/mes_dashboard/core/database.py + src/mes_dashboard/core/redis_client.py" }
  - { type: tdd-pairs, pointer: "tests/test_resource_cache_version_check.py ↔ src/mes_dashboard/services/resource_cache.py" }
  - { type: tdd-pairs, pointer: "tests/integration/test_preload_fork_safety.py ↔ gunicorn.conf.py + src/mes_dashboard/app.py + all prewarm services" }
  - { type: coverage-tiers, pointer: "unit (Tier 1), integration (Tier 3), resilience (Tier 3), contract (Tier 1), soak (Tier 4)" }
  - { type: mapping-completeness, pointer: "all 10 ACs covered; AC-1/2/3/4/5/6/8/9 have Tier 3 integration evidence; AC-7/10 covered by Tier 1 unit/contract; no AC left unmapped" }

notes: |
  Per-test markers required in test_preload_fork_safety.py (not module-level pytestmark) to
  prevent silent skip in default pytest runs. AC-7 tests placed in new Tier 1 file, not in
  test_oracle_error_path.py which carries integration_real pytestmark.
