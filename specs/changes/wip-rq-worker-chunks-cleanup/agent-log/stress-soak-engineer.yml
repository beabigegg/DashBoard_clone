agent: stress-soak-engineer
change: wip-rq-worker-chunks-cleanup
date: 2026-06-20

summary: >
  Verified and fixed test_wip_worker_stress.py stub (thread-safety bug in patch.object
  usage inside worker threads); confirmed both tests pass with mock Oracle.
  Wrote stress-soak-report.md documenting mock wiring evidence and pre-production
  real-Redis gate requirements per ci-gates.md §Pre-Production Manual Gate.

findings:
  - stub-bug: >
      Original stub applied patch.object(svc, ...) inside N=20 concurrent thread functions.
      unittest.mock.patch is not thread-safe under concurrent context-manager enter/exit
      on the same module attribute — some threads saw the real execute_wip_detail_oracle_query
      and raised oracledb DPY-3001 (bequeath thin-mode). Fix: all patches moved to
      test-method level via monkeypatch.setattr (single globally-visible attribute assignment).
  - ac8-coverage: >
      test_burst_peak_bounded_no_leak: N=20, enters=20, exits=20, peak=20, elapsed=0.07s, PASSED.
      test_burst_no_deadlock_with_mixed_success_failure: N=20, completed=16, faulted=4,
      enters=20, exits=20, PASSED. peak==N is expected in mock mode (recording CM has no cap).
  - soak-parametrization: >
      test_soak_workload.py uses a fixed endpoint rotation (_TRAFFIC_ENDPOINTS); WIP detail
      async path is NOT currently in the rotation. WIP soak coverage is a post-activation
      item — add POST /api/wip/detail to _TRAFFIC_ENDPOINTS after Gate 3.

artifacts:
  - { type: runner-config-path, pointer: "tests/stress/test_wip_worker_stress.py" }
  - { type: runner, pointer: "pytest --run-stress" }
  - { type: pass-criteria-cited, pointer: "enters==exits==N=20 (mock wiring); peak<=HEAVY_QUERY_MAX_CONCURRENT=3 (pre-production real-Redis gate); 4 faults/16 success slot leak=0" }
  - { type: artifacts-location, pointer: "specs/changes/wip-rq-worker-chunks-cleanup/stress-soak-report.md" }
