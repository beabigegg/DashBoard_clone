agent: test-strategist
date: 2026-06-20
status: complete

artifacts:
  - { type: test-plan-path, pointer: "specs/changes/wip-rq-worker-chunks-cleanup/test-plan.md" }
  - { type: tdd-pairs, pointer: "tests/test_wip_query_job_service.py → src/mes_dashboard/services/wip_query_job_service.py" }
  - { type: tdd-pairs, pointer: "tests/integration/test_wip_rowcount_rq_routing.py → src/mes_dashboard/routes/wip_routes.py + src/mes_dashboard/services/wip_query_job_service.py" }
  - { type: tdd-pairs, pointer: "tests/test_batch_query_engine.py (TestMergeChunks deleted) → src/mes_dashboard/services/batch_query_engine.py (merge_chunks deleted)" }
  - { type: tdd-pairs, pointer: "tests/test_spool_routes.py → src/mes_dashboard/routes/spool_routes.py (_ALLOWED_NAMESPACES + wip_dataset)" }
  - { type: tdd-pairs, pointer: "tests/test_job_registry.py → src/mes_dashboard/app.py + src/mes_dashboard/services/wip_query_job_service.py" }
  - { type: coverage-tiers, pointer: "unit (tier 0), integration (tier 1), resilience (tier 1), e2e (tier 1), stress (tier 3), soak (tier 4)" }
  - { type: mapping-completeness, pointer: "all 8 ACs mapped; AC-4 and AC-6 each covered by 2 tests; AC-7 open risk noted with xfail placeholder guidance" }

notes: |
  Key finding: existing TestMergeChunks (5 tests, lines 345-495 of test_batch_query_engine.py) must be
  deleted wholesale — not updated. The merge_chunks import on line 11 of that file also requires removal.
  AC-7 schema parity is at risk until backend-engineer resolves summary-vs-lots spool assembly; flagged
  with xfail placeholder pattern. No routing flag introduced (D1) so no env-contract test rows needed
  beyond the _APPROVED_CALLERS update.
