agent: contract-reviewer
change-id: downtime-browser-duckdb
timestamp: 2026-06-12
status: changes-required
summary: >
  All 7 contract surfaces reviewed against design.md + change-classification.md.
  Six contracts must be updated before implementation starts. CHANGELOG requires
  5 new entries. Condition for env-contract (DOWNTIME_BROWSER_DUCKDB feature flag)
  and ci-gate-contract (new Playwright spec) both confirmed triggered.
artifacts:
  - type: version-bumps
    pointer: "api-contract: 1.14.0→1.15.0; api-inventory: 1.1.13→1.2.0; data-shape: 1.12.3→1.13.0; business-rules: 1.16.0→1.17.0; env-contract: 1.0.6→1.0.7; ci-gate: 1.3.19→1.3.20"
  - type: breaking-change
    pointer: "POST /api/downtime-analysis/query removes summary/daily_trend/big_category/top_reasons from live path; /view+/equipment-detail+/event-detail deprecated (removal target api 1.17.0)"
  - type: blocking-items
    notes: >
      (1) api-contract §10 + response shape must be updated before backend-engineer writes the route handler.
      (2) data-shape §3.13 raw-parquet schemas must be written before spool writer is implemented.
      (3) business-rules DA-01..DA-04 locus changes + 90d-removal rule + browser-ceiling rule + two-parquet atomicity rule must be in contract before frontend-engineer writes useDowntimeDuckDB.ts.
      (4) CHANGELOG 5 entries must precede gate validation (cdd-kit validate --versions).
      (5) DOWNTIME_BROWSER_DUCKDB default (true/false at ship) must be decided before backend-engineer sets code default.
      (6) ci-gate-contract rollback policy OOM-risk caveat must be documented before deployment.
  - type: css-ui-confirmation
    notes: "CSS/UI contract unchanged — no token or component styling change; task 2.2 remains skipped"
  - type: key-open-question
    notes: "DOWNTIME_BROWSER_DUCKDB initial default: default-on (true) ships parity risk; default-off (false) ships safely but requires operator action to cut over. backend-engineer must decide and document in env-contract."
