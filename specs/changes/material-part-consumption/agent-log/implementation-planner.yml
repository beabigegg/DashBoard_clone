change-id: material-part-consumption
agent: implementation-planner
timestamp: "2026-05-20T00:00:00Z"
status: complete
artifacts:
  - { type: plan-written, pointer: "specs/changes/material-part-consumption/implementation-plan.md" }
  - { type: owner-map, pointer: "backend-engineer (IP-1..IP-9, IP-13), frontend-engineer (IP-10..IP-12)" }
  - { type: scope-summary, pointer: "7 in-scope areas (backend slice, 2 spools, 7 endpoints, MC-01..04, rq_monitor, frontend app, registration+deploy); 7 out-of-scope (prewarm, DB migration, existing endpoints/spools/pages, shared-ui non-additive, soak/stress pre-merge, design.md authoring)" }
next-action: none
notes: >
  design.md present and fully filled (Architecture Review Required = yes) —
  not a scaffold; no block needed. Plan reuses 3 CER-001 reference impls
  (material_trace_service, material_trace_duckdb_runtime,
  resource_history_sql_runtime). Summary spool cache key EXCLUDES granularity
  (ADR-0001, MC-03). asset_readiness_manifest.json entry must match the actual
  built dist filename — flagged as cross-engineer sequencing risk and gunicorn
  startup crash surface. Parquet schema marked breaking-change surface with
  rollback rm requirement. TDD-first ordering enforced for both engineers.
