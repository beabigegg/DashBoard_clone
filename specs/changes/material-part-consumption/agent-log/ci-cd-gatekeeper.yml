change-id: material-part-consumption
agent: ci-cd-gatekeeper
timestamp: "2026-05-20T00:00:00Z"
status: complete
artifacts:
  - { type: tiers-modified, pointer: "1, 3, 4, 5" }
  - { type: gate-promotions, pointer: "n/a (net-new feature; no existing gates promoted or demoted)" }
  - { type: workflow-files-changed, pointer: "n/a (no workflow files modified; all gates fit existing backend-tests.yml / frontend-tests.yml / soak-tests.yml / stress-tests.yml paths)" }
  - { type: required-status-checks, pointer: "unit-mock-integration, frontend-unit, css-governance, playwright-critical-journeys, playwright-data-boundary, playwright-resilience, fuzz" }
next-action: none
notes: >
  All Tier 1 required gates land in existing workflows via path-filter pickup.
  Tier 3 nightly stress (tests/stress/test_material_consumption_stress.py) and
  Tier 4 weekly soak (test_soak_workload.py extension) are auto-discovered by
  existing commands — no new workflow files needed.
  Tier 5 manual worker-deploy-verify step documented in ci-gates.md as a
  pre-traffic-cutover checklist; not a GHA job.
  Rollback sequence is order-sensitive: asset_readiness_manifest.json removal
  MUST precede gunicorn restart or startup crashes all workers.
