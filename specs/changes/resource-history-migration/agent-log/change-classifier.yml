agent: change-classifier
status: complete
timestamp: "2026-06-19"

summary: |
  Classified resource-history-migration as Tier 1, Risk high, Impact cross-module.
  Architecture review required: dual-query (detail + OEE) structure forces requires_cross_chunk_reduction
  decision that must be resolved by spec-architect before implementation-planner.
  9 ACs derived. Flag RESOURCE_HISTORY_USE_UNIFIED_JOB default off.

artifacts:
  - { type: classification, pointer: specs/changes/resource-history-migration/change-classification.md }
  - { type: context-manifest, pointer: specs/changes/resource-history-migration/context-manifest.md }

key-findings:
  - "Tier 1 confirmed: shared-core blast radius (base_chunked_duckdb_job, query_cost_policy, job_registry)"
  - "Architecture review REQUIRED: OEE Availability = ratio-of-SUMs per RESOURCEID across time chunks → cross-row reduction; cannot naive row-chunk by time"
  - "Key design decision: one True job (post_aggregate produces both detail + OEE views) vs two separate jobs (False detail + True OEE)"
  - "AC-3 is highest-risk: OEE cross-chunk numeric parity ≤1e-6"
  - "No new API endpoints; spool schema unchanged; no frontend change"
  - "tier-floor-override required: same rationale as prior P2 (flag-gated off, no default-path caller)"
  - "eap-alarm-unified-job-poc precondition satisfied: base_chunked_duckdb_job.py already in codebase"
