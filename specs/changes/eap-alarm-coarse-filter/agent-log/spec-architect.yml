agent: spec-architect
change-id: eap-alarm-coarse-filter
timestamp: "2026-06-30"
status: complete
summary: >
  Wrote design.md (under 150 lines) for the EAP-alarm coarse-filter expansion.
  Recorded D-1 (5-dim spool-key hash, empty-list-as-empty-string to prevent
  cross-dim collision), D-2 (reuse container_filter_cache for product-filter-options),
  D-3 (EXISTS semi-join not LEFT JOIN+DISTINCT — no row explosion), D-4 (at-least-one-
  of-three validated route-side pre-key), D-5 (_SCHEMA_VERSION 2→3 self-invalidating).
  Extends ADR 0008 rather than opening a new boundary; flagged ADR 0008 for amendment
  (still proposed, still says coarse-only sha256(eqp_types)). No new ADR written.
artifacts:
  - { type: adr-written, pointer: "none — extends docs/adr/0008; flagged for owner amendment in design.md Open Risks" }
  - { type: affected-areas, pointer: "eap-alarm spool-key, oracle worker query, route validation, container_filter_cache, frontend filter bar" }
  - { type: decision-summary, pointer: "5-dim spool key + EXISTS semi-join + at-least-one-of-three validation + schema_version 2→3" }
  - { type: risks-noted, pointer: "0 high, 3 medium (ADR-0008 amendment, CHAR-pad TRIM parity, cold-cache empty-options UX)" }
notes: >
  Contracts (data §3.17, business EA-01/08/09/10, api) were already drafted by
  contract-reviewer and are consistent with the recorded decisions — design.md
  references them rather than restating. No SQL/code blocks written.
