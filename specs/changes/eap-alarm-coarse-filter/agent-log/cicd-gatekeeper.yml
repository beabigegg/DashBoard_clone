agent: cicd-gatekeeper
change-id: eap-alarm-coarse-filter
timestamp: "2026-06-30"
status: complete
summary: >
  Wrote ci-gates.md for the EAP-alarm coarse-filter expansion (Tier 2, medium-risk).
  No new workflow YAML or Makefile targets required; change-classification confirmed
  existing eap-alarm CI gates cover all new test surfaces. Gate table enumerates
  11 gates (Tier 0-3), referencing test-plan.md rows by AC-id. Rollback policy
  records the _SCHEMA_VERSION 2->3 revert + parquet rm sequence. Merge eligibility:
  mergeable pending 7 Tier-1 required-status checks.
artifacts:
  - { type: tiers-modified, pointer: "0, 1, 3" }
  - { type: gate-promotions, pointer: "n/a (no existing gates moved between tiers)" }
  - { type: workflow-files-changed, pointer: "n/a (no workflow files modified; existing ci.yml + nightly.yml sufficient)" }
  - { type: required-status-checks, pointer: "unit-and-integration-tests, frontend-unit-tests, css-check, type-check, contract-validation, openapi-sync, playwright-e2e" }
