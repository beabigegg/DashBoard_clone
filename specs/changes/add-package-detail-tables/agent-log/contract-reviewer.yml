agent: contract-reviewer
change-id: add-package-detail-tables
timestamp: 2026-05-22
status: changes-applied

artifacts:
  - type: contracts-reviewed
    pointer: "contracts/api/api-contract.md, contracts/api/api-inventory.md, contracts/data/data-shape-contract.md, contracts/css/css-contract.md"
  - type: version-bumps
    pointer: "api-contract: 1.10.0 → 1.11.0; api-inventory: 1.1.9 → 1.1.10; data-shape-contract: 1.9.0 → 1.10.0; css-contract: no-change"
  - type: breaking-changes
    pointer: "none — all 5 endpoint changes are additive; equipment-rejects PRODUCTLINENAME was already in SQL and §3.7; material-consumption detail spool schema change requires parquet cleanup on deploy/rollback (documented in §3.9.2 and ci-gates.md)"
  - type: key-finding
    pointer: "_PARTIAL_NONKEY_COLS_LOT in query_tool_sql_runtime.py must include PRODUCTLINENAME for QT-06 strict guard correctness"
  - type: new-section
    pointer: "contracts/data/data-shape-contract.md §3.11 — hold-history detail row schema (baseline documentation debt; this section did not exist before)"
