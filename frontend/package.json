{
  "name": "mes-dashboard-frontend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "test": "node --test tests/*.test.js"
  },
  "devDependencies": {
    "vite": "^6.3.0"
  }
}
